import { Story } from "@storybook/addon-docs";

<Story
  name="Primary"
  parameters={{

}}
args={{
     title: "title",
    actionName: "Save",
    action: () => alert("Click"),
  }}>
{Template.bind({})}

</Story>
